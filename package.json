{
  "name": "resumeai-dev",
  "private": true,
  "scripts": {
    "dev": "concurrently \"cd frontend && npm run dev\" \"cd backend && mvnw.cmd spring-boot:run\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
